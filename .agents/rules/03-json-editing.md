---
trigger: glob
globs: lhm.plugin.json, package.json
---
При редактировании файлов манифестов строго сохраняй их структуру и порядок ключей, если таковой был задан изначально. Проверяй синтаксис JSON перед сохранением и используй специализированные инструменты (например, `replace_file_content` для точечных замен) вместо полной перезаписи файла, чтобы не нарушить конфигурацию.
